// Generated by CoffeeScript 1.10.0
(function() {
  var Cells, changeState, countLiveNeighbours, ctx, height, nocells, randomInit, size, thing, tick, updateSquare, width;

  height = 800;

  width = 800;

  size = 10;

  nocells = 73;

  ctx = document.getElementById("myCanvas").getContext("2d");

  ctx.fillStyle = 'rgb(0,0,255)';

  ctx.fillRect(0, 0, width, height);

  Cells = [];

  randomInit = function() {
    var A, a, color, k, ref, results, thing, x, y;
    results = [];
    for (x = k = 0, ref = nocells; 0 <= ref ? k < ref : k > ref; x = 0 <= ref ? ++k : --k) {
      Cells[x] = [];
      results.push((function() {
        var l, ref1, results1;
        results1 = [];
        for (y = l = 0, ref1 = nocells; 0 <= ref1 ? l < ref1 : l > ref1; y = 0 <= ref1 ? ++l : --l) {
          a = Math.random();
          if (a > 0.5) {
            thing = 'alive';
            color = 'rgb(0,0,100)';
          } else {
            thing = 'dead';
            color = 'rgb(0,0,0)';
          }
          ctx.fillStyle = color;
          ctx.fillRect(x + x * size, y + y * size, size, size);
          A = {
            'state': thing,
            'row': x,
            'column': y,
            'neighbors': 0
          };
          results1.push(Cells[x][y] = A);
        }
        return results1;
      })());
    }
    return results;
  };

  updateSquare = function(Cells) {
    var color, k, ref, results, x, y;
    results = [];
    for (x = k = 0, ref = nocells; 0 <= ref ? k < ref : k > ref; x = 0 <= ref ? ++k : --k) {
      results.push((function() {
        var l, ref1, results1;
        results1 = [];
        for (y = l = 0, ref1 = nocells; 0 <= ref1 ? l < ref1 : l > ref1; y = 0 <= ref1 ? ++l : --l) {
          if (Cells[x][y]['state'] === 'alive') {
            color = 'rgb(0,0,100)';
          } else if (Cells[x][y]['state'] === 'dead') {
            color = 'rgb(0,0,0)';
          }
          ctx.fillStyle = color;
          results1.push(ctx.fillRect(x + x * size, y + y * size, size, size));
        }
        return results1;
      })());
    }
    return results;
  };

  countLiveNeighbours = function(Cells, i, j) {
    var collb, colub, k, l, neighbors, ref, ref1, ref2, ref3, rowlb, rowub, x, y;
    neighbors = 0;
    rowlb = Math.max(j - 1, 0);
    rowub = Math.min(j + 1, nocells - 1);
    collb = Math.max(i - 1, 0);
    colub = Math.min(i + 1, nocells - 1);
    for (x = k = ref = collb, ref1 = colub; ref <= ref1 ? k <= ref1 : k >= ref1; x = ref <= ref1 ? ++k : --k) {
      for (y = l = ref2 = rowlb, ref3 = rowub; ref2 <= ref3 ? l <= ref3 : l >= ref3; y = ref2 <= ref3 ? ++l : --l) {
        if (!(x === i && y === j)) {
          if (Cells[x][y]['state'] === 'alive') {
            neighbors++;
          }
        }
      }
    }
    return Cells[i][j]['neighbors'] = neighbors;
  };

  changeState = function(Cells) {
    var i, j, k, ref, results;
    results = [];
    for (i = k = 0, ref = nocells; 0 <= ref ? k < ref : k > ref; i = 0 <= ref ? ++k : --k) {
      results.push((function() {
        var l, ref1, results1;
        results1 = [];
        for (j = l = 0, ref1 = nocells; 0 <= ref1 ? l < ref1 : l > ref1; j = 0 <= ref1 ? ++l : --l) {
          if (Cells[i][j]['state'] === 'alive' && Cells[i][j]['neighbors'] < 2) {
            Cells[i][j]['state'] = 'dead';
          }
          if (Cells[i][j]['state'] === 'alive' && Cells[i][j]['neighbors'] > 3) {
            Cells[i][j]['state'] = 'dead';
          }
          if (Cells[i][j]['state'] === 'dead' && Cells[i][j]['neighbors'] === 3) {
            results1.push(Cells[i][j]['state'] = 'alive');
          } else {
            results1.push(void 0);
          }
        }
        return results1;
      })());
    }
    return results;
  };

  tick = function() {
    var i, j, k, l, ref, ref1;
    for (i = k = 0, ref = nocells; 0 <= ref ? k < ref : k > ref; i = 0 <= ref ? ++k : --k) {
      for (j = l = 0, ref1 = nocells; 0 <= ref1 ? l < ref1 : l > ref1; j = 0 <= ref1 ? ++l : --l) {
        countLiveNeighbours(Cells, i, j);
      }
    }
    changeState(Cells);
    updateSquare(Cells);
    return setTimeout(tick, 100);
  };

  thing = function() {
    return alert("hi");
  };

  randomInit();

  tick();

}).call(this);
